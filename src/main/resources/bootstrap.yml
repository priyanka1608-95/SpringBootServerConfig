spring:
  application:
    name: practice-client
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
